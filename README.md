# vite-electron-vue-ts

A starter template for building Electron applications using Vite, Vue 3, and TypeScript.

## Note
- `electron-builder` does not support `pnpm` catalogs, so Electron-related dependencies are currently specified directly in package.json with explicit versions.
